import React from "react"

const Offers = () => (
  <div className="offers-section">
    <div
      className="container"
      style={{ flexDirection: `column`, padding: `2rem 1rem` }}
    >
        
  </div>
)

export default Offers
